-- ⚡ 51S DUELS ⚡
-- Purple Edition — Full Feature Build

task.wait(0.1)
repeat task.wait() until game:IsLoaded()

local CoreGui          = game:GetService("CoreGui")
local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService     = game:GetService("TweenService")
local Lighting         = game:GetService("Lighting")
local Player           = Players.LocalPlayer

local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

-- ============================================================
-- SAFE CHARACTER WAIT
-- ============================================================
local function waitForCharacter()
    local char = Player.Character
    if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChildOfClass("Humanoid") then return char end
    return Player.CharacterAdded:Wait()
end
task.spawn(waitForCharacter)
if not getgenv then getgenv = function() return _G end end

-- ============================================================
-- STATE
-- ============================================================
local Enabled = {
    SpeedBoost         = false,
    AntiRagdoll        = false,
    HitCircle          = false,
    Float              = false,
    SpeedWhileStealing = false,
    AutoSteal          = false,
    Unwalk             = false,
    OptimizerXRay      = false,
    SpamBat            = false,
    Helicopter         = false,
    Vibrance           = false,
    AutoRight          = false,
    AutoLeft           = false,
    GalaxyMode         = false,
}

local Values = {
    BoostSpeed           = 30,
    SpinSpeed            = 30,
    StealingSpeedValue   = 29,
    STEAL_RADIUS         = 20,
    STEAL_DURATION       = 1.3,
    DEFAULT_GRAVITY      = 196.2,
    GalaxyGravityPercent = 70,
    HOP_POWER            = 35,
    HOP_COOLDOWN         = 0.08,
}

local KEYBINDS = {
    SPEED     = Enum.KeyCode.V,
    FLOAT     = Enum.KeyCode.F,
    AUTORIGHT = Enum.KeyCode.E,
    AUTOLEFT  = Enum.KeyCode.Q,
}

local Connections        = {}
local isStealing         = false
local lastBatSwing       = 0
local BAT_SWING_COOLDOWN = 0.12

-- ============================================================
-- HELPERS
-- ============================================================
local function getMovementDirection()
    local c = Player.Character
    if not c then return Vector3.zero end
    local hum = c:FindFirstChildOfClass("Humanoid")
    return hum and hum.MoveDirection or Vector3.zero
end

local SlapList = {
    {1,"Bat"},{2,"Slap"},{3,"Iron Slap"},{4,"Gold Slap"},
    {5,"Diamond Slap"},{6,"Emerald Slap"},{7,"Ruby Slap"},
    {8,"Dark Matter Slap"},{9,"Flame Slap"},{10,"Nuclear Slap"},
    {11,"Galaxy Slap"},{12,"Glitched Slap"}
}

local function findBat()
    local c = Player.Character
    if not c then return nil end
    local bp = Player:FindFirstChildOfClass("Backpack")
    for _, ch in ipairs(c:GetChildren()) do
        if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end
    end
    if bp then
        for _, ch in ipairs(bp:GetChildren()) do
            if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end
        end
    end
    for _, i in ipairs(SlapList) do
        local t = c:FindFirstChild(i[2]) or (bp and bp:FindFirstChild(i[2]))
        if t then return t end
    end
    return nil
end

-- ============================================================
-- FEATURE LOGIC
-- ============================================================

-- Speed Boost
local function startSpeedBoost()
    if Connections.speed then return end
    Connections.speed = RunService.Heartbeat:Connect(function()
        if not Enabled.SpeedBoost then return end
        pcall(function()
            local c = Player.Character
            if not c then return end
            local h = c:FindFirstChild("HumanoidRootPart")
            if not h then return end
            local md = getMovementDirection()
            if md.Magnitude > 0.1 then
                h.AssemblyLinearVelocity = Vector3.new(md.X * Values.BoostSpeed, h.AssemblyLinearVelocity.Y, md.Z * Values.BoostSpeed)
            end
        end)
    end)
end
local function stopSpeedBoost()
    if Connections.speed then Connections.speed:Disconnect() Connections.speed = nil end
end

-- Speed While Stealing
local function startSpeedWhileStealing()
    if Connections.speedWhileStealing then return end
    Connections.speedWhileStealing = RunService.Heartbeat:Connect(function()
        if not Enabled.SpeedWhileStealing or not Player:GetAttribute("Stealing") then return end
        local c = Player.Character
        if not c then return end
        local h = c:FindFirstChild("HumanoidRootPart")
        if not h then return end
        local md = getMovementDirection()
        if md.Magnitude > 0.1 then
            h.AssemblyLinearVelocity = Vector3.new(md.X * Values.StealingSpeedValue, h.AssemblyLinearVelocity.Y, md.Z * Values.StealingSpeedValue)
        end
    end)
end
local function stopSpeedWhileStealing()
    if Connections.speedWhileStealing then Connections.speedWhileStealing:Disconnect() Connections.speedWhileStealing = nil end
end

-- Anti Ragdoll
local antiRagdollMode    = nil
local ragdollConnections = {}
local cachedCharData     = {}
local isBoosting         = false
local AR_BOOST_SPEED     = 400
local AR_DEFAULT_SPEED   = 16

local function arCacheCharacterData()
    local char = Player.Character
    if not char then return false end
    local hum  = char:FindFirstChildOfClass("Humanoid")
    local root = char:FindFirstChild("HumanoidRootPart")
    if not hum or not root then return false end
    cachedCharData = { character = char, humanoid = hum, root = root }
    return true
end
local function arDisconnectAll()
    for _, conn in ipairs(ragdollConnections) do pcall(function() conn:Disconnect() end) end
    ragdollConnections = {}
end
local function arIsRagdolled()
    if not cachedCharData.humanoid then return false end
    local state = cachedCharData.humanoid:GetState()
    if state == Enum.HumanoidStateType.Physics or
       state == Enum.HumanoidStateType.Ragdoll or
       state == Enum.HumanoidStateType.FallingDown then return true end
    local endTime = Player:GetAttribute("RagdollEndTime")
    if endTime and (endTime - workspace:GetServerTimeNow()) > 0 then return true end
    return false
end
local function arForceExit()
    if not cachedCharData.humanoid or not cachedCharData.root then return end
    pcall(function() Player:SetAttribute("RagdollEndTime", workspace:GetServerTimeNow()) end)
    for _, d in ipairs(cachedCharData.character:GetDescendants()) do
        if d:IsA("BallSocketConstraint") or (d:IsA("Attachment") and d.Name:find("RagdollAttachment")) then d:Destroy() end
    end
    if not isBoosting then
        isBoosting = true
        cachedCharData.humanoid.WalkSpeed = AR_BOOST_SPEED
    end
    if cachedCharData.humanoid.Health > 0 then
        cachedCharData.humanoid:ChangeState(Enum.HumanoidStateType.Running)
    end
    cachedCharData.root.Anchored = false
end
local function arHeartbeatLoop()
    while antiRagdollMode == "v1" do
        task.wait()
        if not Enabled.AntiRagdoll then break end
        local ragdolled = arIsRagdolled()
        if ragdolled then
            arForceExit()
        elseif isBoosting and not ragdolled then
            isBoosting = false
            if cachedCharData.humanoid then cachedCharData.humanoid.WalkSpeed = AR_DEFAULT_SPEED end
        end
    end
end
local function startAntiRagdoll()
    if antiRagdollMode == "v1" then return end
    if not arCacheCharacterData() then return end
    antiRagdollMode = "v1"
    local camConn = RunService.RenderStepped:Connect(function()
        local cam = workspace.CurrentCamera
        if cam and cachedCharData.humanoid then cam.CameraSubject = cachedCharData.humanoid end
    end)
    table.insert(ragdollConnections, camConn)
    local respawnConn = Player.CharacterAdded:Connect(function()
        isBoosting = false
        task.wait(0.5)
        arCacheCharacterData()
    end)
    table.insert(ragdollConnections, respawnConn)
    task.spawn(arHeartbeatLoop)
end
local function stopAntiRagdoll()
    antiRagdollMode = nil
    if isBoosting and cachedCharData.humanoid then cachedCharData.humanoid.WalkSpeed = AR_DEFAULT_SPEED end
    isBoosting = false
    arDisconnectAll()
    cachedCharData = {}
end

-- Spam Bat
local function startSpamBat()
    if Connections.spamBat then return end
    Connections.spamBat = RunService.Heartbeat:Connect(function()
        if not Enabled.SpamBat then return end
        local c = Player.Character
        if not c then return end
        local bat = findBat()
        if not bat then return end
        if bat.Parent ~= c then bat.Parent = c end
        local now = tick()
        if now - lastBatSwing < BAT_SWING_COOLDOWN then return end
        lastBatSwing = now
        pcall(function() bat:Activate() end)
    end)
end
local function stopSpamBat()
    if Connections.spamBat then Connections.spamBat:Disconnect() Connections.spamBat = nil end
end

-- Helicopter / Spin Bot
local helicopterBAV = nil
local function startHelicopter()
    local c = Player.Character
    if not c then return end
    local hrp = c:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    if helicopterBAV then helicopterBAV:Destroy() helicopterBAV = nil end
    helicopterBAV = Instance.new("BodyAngularVelocity")
    helicopterBAV.Name = "HelicopterBAV"
    helicopterBAV.MaxTorque = Vector3.new(0, math.huge, 0)
    helicopterBAV.AngularVelocity = Vector3.new(0, Values.SpinSpeed, 0)
    helicopterBAV.Parent = hrp
end
local function stopHelicopter()
    if helicopterBAV then helicopterBAV:Destroy() helicopterBAV = nil end
    local c = Player.Character
    if c then
        local hrp = c:FindFirstChild("HumanoidRootPart")
        if hrp then
            for _, v in pairs(hrp:GetChildren()) do
                if v.Name == "HelicopterBAV" then v:Destroy() end
            end
        end
    end
end

-- Float
local floatConn = nil
local FLOAT_TARGET_HEIGHT = 10
local function startFloat()
    if floatConn then return end
    local c = Player.Character
    if not c then return end
    local hrp = c:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local floatOriginY   = hrp.Position.Y + FLOAT_TARGET_HEIGHT
    local floatStartTime = tick()
    local floatDescending = false
    floatConn = RunService.Heartbeat:Connect(function()
        if not Enabled.Float then return end
        local c2 = Player.Character
        if not c2 then return end
        local h = c2:FindFirstChild("HumanoidRootPart")
        if not h then return end
        local hum2 = c2:FindFirstChildOfClass("Humanoid")
        local moveDir = hum2 and hum2.MoveDirection or Vector3.zero
        if tick() - floatStartTime >= 4 then floatDescending = true end
        local currentY = h.Position.Y
        local vertVel
        if floatDescending then
            vertVel = -20
            if currentY <= floatOriginY - FLOAT_TARGET_HEIGHT + 0.5 then
                h.AssemblyLinearVelocity = Vector3.zero
                Enabled.Float = false
                if floatConn then floatConn:Disconnect() floatConn = nil end
                return
            end
        else
            local diff = floatOriginY - currentY
            if diff > 0.3 then vertVel = math.clamp(diff * 8, 5, 50)
            elseif diff < -0.3 then vertVel = math.clamp(diff * 8, -50, -5)
            else vertVel = 0 end
        end
        local horizX = moveDir.Magnitude > 0.1 and moveDir.X * Values.BoostSpeed or 0
        local horizZ = moveDir.Magnitude > 0.1 and moveDir.Z * Values.BoostSpeed or 0
        h.AssemblyLinearVelocity = Vector3.new(horizX, vertVel, horizZ)
    end)
end
local function stopFloat()
    if floatConn then floatConn:Disconnect() floatConn = nil end
    local c = Player.Character
    if c then
        local hrp = c:FindFirstChild("HumanoidRootPart")
        if hrp then hrp.AssemblyLinearVelocity = Vector3.zero end
    end
end

-- Unwalk
local savedAnimations = {}
local function startUnwalk()
    local c = Player.Character
    if not c then return end
    local hum = c:FindFirstChildOfClass("Humanoid")
    if hum then for _, t in ipairs(hum:GetPlayingAnimationTracks()) do t:Stop() end end
    local anim = c:FindFirstChild("Animate")
    if anim then savedAnimations.Animate = anim:Clone() anim:Destroy() end
end
local function stopUnwalk()
    local c = Player.Character
    if c and savedAnimations.Animate then
        savedAnimations.Animate:Clone().Parent = c
        savedAnimations.Animate = nil
    end
end

-- Hit Circle (Melee Aimbot)
local Cebo = { Conn=nil, Circle=nil, Align=nil, Attach=nil }
local function startHitCircle()
    if Cebo.Conn then return end
    local char = Player.Character or Player.CharacterAdded:Wait()
    local hrp = char:WaitForChild("HumanoidRootPart")
    Cebo.Attach = Instance.new("Attachment", hrp)
    Cebo.Align = Instance.new("AlignOrientation", hrp)
    Cebo.Align.Attachment0 = Cebo.Attach
    Cebo.Align.Mode = Enum.OrientationAlignmentMode.OneAttachment
    Cebo.Align.RigidityEnabled = true
    Cebo.Circle = Instance.new("Part")
    Cebo.Circle.Shape = Enum.PartType.Cylinder
    Cebo.Circle.Material = Enum.Material.Neon
    Cebo.Circle.Size = Vector3.new(0.05, 14.5, 14.5)
    Cebo.Circle.Color = Color3.fromRGB(160, 60, 255)
    Cebo.Circle.CanCollide = false
    Cebo.Circle.Massless = true
    Cebo.Circle.Parent = workspace
    local weld = Instance.new("Weld")
    weld.Part0 = hrp
    weld.Part1 = Cebo.Circle
    weld.C0 = CFrame.new(0, -1, 0) * CFrame.Angles(0, 0, math.rad(90))
    weld.Parent = Cebo.Circle
    Cebo.Conn = RunService.RenderStepped:Connect(function()
        if not Enabled.HitCircle then return end
        local target, dmin = nil, 7.25
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= Player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local d = (p.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                if d <= dmin then target, dmin = p.Character.HumanoidRootPart, d end
            end
        end
        if target then
            char.Humanoid.AutoRotate = false
            Cebo.Align.Enabled = true
            Cebo.Align.CFrame = CFrame.lookAt(hrp.Position, Vector3.new(target.Position.X, hrp.Position.Y, target.Position.Z))
            local t = char:FindFirstChild("Bat") or char:FindFirstChild("Medusa")
            if t then t:Activate() end
        else
            Cebo.Align.Enabled = false
            char.Humanoid.AutoRotate = true
        end
    end)
end
local function stopHitCircle()
    if Cebo.Conn   then Cebo.Conn:Disconnect()   Cebo.Conn   = nil end
    if Cebo.Circle then Cebo.Circle:Destroy()     Cebo.Circle = nil end
    if Cebo.Align  then Cebo.Align:Destroy()      Cebo.Align  = nil end
    if Cebo.Attach then Cebo.Attach:Destroy()     Cebo.Attach = nil end
    if Player.Character and Player.Character:FindFirstChild("Humanoid") then
        Player.Character.Humanoid.AutoRotate = true
    end
end

-- Galaxy / Gravity Mode
local galaxyModeEnabled  = false
local galaxyHopsEnabled  = false
local galaxyLastHopTime  = 0
local galaxySpaceHeld    = false
local galaxyOriginalJump = 50
local galaxyVectorForce  = nil
local galaxyAttachment   = nil

local function galaxyCaptureJumpPower()
    local c = Player.Character
    if c then
        local hum = c:FindFirstChildOfClass("Humanoid")
        if hum and hum.JumpPower > 0 then galaxyOriginalJump = hum.JumpPower end
    end
end
task.spawn(function() task.wait(1) galaxyCaptureJumpPower() end)
Player.CharacterAdded:Connect(function() task.wait(1) galaxyCaptureJumpPower() end)

local function galaxySetupForce()
    pcall(function()
        local c = Player.Character
        if not c then return end
        local h = c:FindFirstChild("HumanoidRootPart")
        if not h then return end
        if galaxyVectorForce then galaxyVectorForce:Destroy() end
        if galaxyAttachment  then galaxyAttachment:Destroy()  end
        galaxyAttachment = Instance.new("Attachment")
        galaxyAttachment.Parent = h
        galaxyVectorForce = Instance.new("VectorForce")
        galaxyVectorForce.Attachment0 = galaxyAttachment
        galaxyVectorForce.ApplyAtCenterOfMass = true
        galaxyVectorForce.RelativeTo = Enum.ActuatorRelativeTo.World
        galaxyVectorForce.Force = Vector3.zero
        galaxyVectorForce.Parent = h
    end)
end
local function galaxyUpdateForce()
    if not galaxyModeEnabled or not galaxyVectorForce then return end
    local c = Player.Character
    if not c then return end
    local mass = 0
    for _, p in ipairs(c:GetDescendants()) do
        if p:IsA("BasePart") then mass += p:GetMass() end
    end
    local tg = Values.DEFAULT_GRAVITY * (Values.GalaxyGravityPercent / 100)
    galaxyVectorForce.Force = Vector3.new(0, mass * (Values.DEFAULT_GRAVITY - tg) * 0.95, 0)
end
local function galaxyAdjustJump()
    pcall(function()
        local c = Player.Character
        if not c then return end
        local hum = c:FindFirstChildOfClass("Humanoid")
        if not hum then return end
        if not galaxyModeEnabled then hum.JumpPower = galaxyOriginalJump return end
        local ratio = math.sqrt((Values.DEFAULT_GRAVITY * (Values.GalaxyGravityPercent / 100)) / Values.DEFAULT_GRAVITY)
        hum.JumpPower = galaxyOriginalJump * ratio
    end)
end
local function galaxyDoMiniHop()
    if not galaxyHopsEnabled then return end
    pcall(function()
        local c = Player.Character
        if not c then return end
        local h = c:FindFirstChild("HumanoidRootPart")
        local hum = c:FindFirstChildOfClass("Humanoid")
        if not h or not hum then return end
        if tick() - galaxyLastHopTime < Values.HOP_COOLDOWN then return end
        galaxyLastHopTime = tick()
        if hum.FloorMaterial == Enum.Material.Air then
            h.AssemblyLinearVelocity = Vector3.new(h.AssemblyLinearVelocity.X, Values.HOP_POWER, h.AssemblyLinearVelocity.Z)
        end
    end)
end
local function startGalaxyMode()
    galaxyModeEnabled = true
    galaxyHopsEnabled = true
    galaxySetupForce()
    galaxyAdjustJump()
end
local function stopGalaxyMode()
    galaxyModeEnabled = false
    galaxyHopsEnabled = false
    if galaxyVectorForce then galaxyVectorForce:Destroy() galaxyVectorForce = nil end
    if galaxyAttachment  then galaxyAttachment:Destroy()  galaxyAttachment  = nil end
    galaxyAdjustJump()
end

RunService.Heartbeat:Connect(function()
    if galaxyHopsEnabled and galaxySpaceHeld then galaxyDoMiniHop() end
    if galaxyModeEnabled then galaxyUpdateForce() end
end)
UserInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and input.KeyCode == Enum.KeyCode.Space then galaxySpaceHeld = true end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Space then galaxySpaceHeld = false end
end)
Player.CharacterAdded:Connect(function()
    task.wait(1)
    if galaxyModeEnabled then galaxySetupForce() galaxyAdjustJump() end
end)

-- Optimizer + XRay
local originalTransparency = {}
local function enableOptimizer()
    if getgenv and getgenv().OPTIMIZER_ACTIVE then return end
    if getgenv then getgenv().OPTIMIZER_ACTIVE = true end
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        Lighting.GlobalShadows = false
        Lighting.Brightness = 3
        Lighting.FogEnd = 9e9
    end)
    pcall(function()
        for _, obj in ipairs(workspace:GetDescendants()) do
            pcall(function()
                if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then
                    obj:Destroy()
                elseif obj:IsA("BasePart") then
                    obj.CastShadow = false
                    obj.Material = Enum.Material.Plastic
                end
            end)
        end
    end)
end
local function disableOptimizer()
    if getgenv then getgenv().OPTIMIZER_ACTIVE = false end
end
local function enableXRay()
    pcall(function()
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") and obj.Anchored and
               (obj.Name:lower():find("base") or (obj.Parent and obj.Parent.Name:lower():find("base"))) then
                originalTransparency[obj] = obj.LocalTransparencyModifier
                obj.LocalTransparencyModifier = 0.85
            end
        end
    end)
end
local function disableXRay()
    for part, value in pairs(originalTransparency) do
        if part then part.LocalTransparencyModifier = value end
    end
    originalTransparency = {}
end

-- Purple Vibe
local purpleVibeCC    = nil
local purpleVibeBloom = nil
local purpleVibeConn  = nil
local purpleVibeFOV   = nil
local purpleVibeBall  = nil

local function enablePurpleVibe()
    if purpleVibeCC then return end
    purpleVibeCC = Instance.new("ColorCorrectionEffect")
    purpleVibeCC.TintColor = Color3.fromRGB(180, 80, 255)
    purpleVibeCC.Saturation = 0.5
    purpleVibeCC.Contrast   = 0.15
    purpleVibeCC.Brightness = 0.05
    purpleVibeCC.Parent = Lighting

    purpleVibeBloom = Instance.new("BloomEffect")
    purpleVibeBloom.Intensity = 1.2
    purpleVibeBloom.Size      = 20
    purpleVibeBloom.Threshold = 0.7
    purpleVibeBloom.Parent = Lighting

    Lighting.ExposureCompensation = 0.2
    Lighting.Ambient = Color3.fromRGB(30, 10, 60)
    Lighting.OutdoorAmbient = Color3.fromRGB(60, 20, 100)

    purpleVibeBall = Instance.new("Part")
    purpleVibeBall.Name       = "PurpleVibe"
    purpleVibeBall.Anchored   = true
    purpleVibeBall.CanCollide = false
    purpleVibeBall.CastShadow = false
    purpleVibeBall.Massless   = true
    purpleVibeBall.Material   = Enum.Material.Neon
    purpleVibeBall.Shape      = Enum.PartType.Ball
    purpleVibeBall.Color      = Color3.fromRGB(160, 60, 255)
    purpleVibeBall.Size       = Vector3.new(60, 60, 60)
    purpleVibeBall.Position   = Vector3.new(0, 800, -500)
    purpleVibeBall.Parent     = workspace

    purpleVibeFOV = Instance.new("ImageLabel")
    purpleVibeFOV.Name = "PurpleVibeFOV"
    purpleVibeFOV.Size = UDim2.new(1, 0, 1, 0)
    purpleVibeFOV.BackgroundTransparency = 1
    purpleVibeFOV.Image = "rbxassetid://6923796549"
    purpleVibeFOV.ImageColor3 = Color3.fromRGB(140, 40, 255)
    purpleVibeFOV.ImageTransparency = 0.55
    purpleVibeFOV.ZIndex = 1
    purpleVibeFOV.Parent = sg

    purpleVibeConn = RunService.Heartbeat:Connect(function()
        if not Enabled.Vibrance then return end
        local t = tick()
        local pulse = math.sin(t * 0.9) * 0.08
        if purpleVibeCC then
            purpleVibeCC.Saturation = 0.45 + pulse
            purpleVibeCC.Brightness = 0.04 + pulse * 0.3
        end
        if purpleVibeBloom then
            purpleVibeBloom.Intensity = 1.1 + math.sin(t * 1.3) * 0.3
        end
        if purpleVibeFOV then
            purpleVibeFOV.ImageTransparency = 0.52 + math.sin(t * 0.7) * 0.06
        end
        if purpleVibeBall then
            purpleVibeBall.Color = Color3.fromRGB(
                math.floor(120 + math.sin(t * 0.5) * 40),
                math.floor(20  + math.sin(t * 0.8) * 20),
                255
            )
        end
    end)
end
local function disablePurpleVibe()
    if purpleVibeConn  then purpleVibeConn:Disconnect()  purpleVibeConn  = nil end
    if purpleVibeBloom then purpleVibeBloom:Destroy()    purpleVibeBloom = nil end
    if purpleVibeCC    then purpleVibeCC:Destroy()       purpleVibeCC    = nil end
    if purpleVibeBall  then purpleVibeBall:Destroy()     purpleVibeBall  = nil end
    if purpleVibeFOV   then purpleVibeFOV:Destroy()      purpleVibeFOV   = nil end
    Lighting.ExposureCompensation = 0
    Lighting.Ambient = Color3.fromRGB(70, 70, 70)
    Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
end

-- Auto Steal
local StealData            = {}
local stealStartTime       = nil
local progressConnection   = nil
local ProgressBarFill      = nil
local ProgressLabel        = nil
local ProgressPercentLabel = nil

local function isMyPlotByName(pn)
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return false end
    local plot = plots:FindFirstChild(pn)
    if not plot then return false end
    local sign = plot:FindFirstChild("PlotSign")
    if sign then
        local yb = sign:FindFirstChild("YourBase")
        if yb and yb:IsA("BillboardGui") then return yb.Enabled == true end
    end
    return false
end
local function findNearestPrompt()
    local h = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if not h then return nil end
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    local np, nd, nn = nil, math.huge, nil
    for _, plot in ipairs(plots:GetChildren()) do
        if isMyPlotByName(plot.Name) then continue end
        local podiums = plot:FindFirstChild("AnimalPodiums")
        if not podiums then continue end
        for _, pod in ipairs(podiums:GetChildren()) do
            pcall(function()
                local base  = pod:FindFirstChild("Base")
                local spawn = base and base:FindFirstChild("Spawn")
                if spawn then
                    local dist = (spawn.Position - h.Position).Magnitude
                    if dist < nd and dist <= Values.STEAL_RADIUS then
                        local att = spawn:FindFirstChild("PromptAttachment")
                        if att then
                            for _, ch in ipairs(att:GetChildren()) do
                                if ch:IsA("ProximityPrompt") then
                                    np, nd, nn = ch, dist, pod.Name
                                    break
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
    return np, nd, nn
end
local function ResetProgressBar()
    if ProgressLabel        then ProgressLabel.Text = "51S DUELS" end
    if ProgressPercentLabel then ProgressPercentLabel.Text = "" end
    if ProgressBarFill      then ProgressBarFill.Size = UDim2.new(0, 0, 1, 0) end
end
local function executeSteal(prompt, name)
    if isStealing then return end
    if not StealData[prompt] then
        StealData[prompt] = {hold = {}, trigger = {}, ready = true}
        pcall(function()
            if getconnections then
                for _, c in ipairs(getconnections(prompt.PromptButtonHoldBegan)) do
                    if c.Function then table.insert(StealData[prompt].hold, c.Function) end
                end
                for _, c in ipairs(getconnections(prompt.Triggered)) do
                    if c.Function then table.insert(StealData[prompt].trigger, c.Function) end
                end
            end
        end)
    end
    local data = StealData[prompt]
    if not data.ready then return end
    data.ready = false
    isStealing = true
    stealStartTime = tick()
    if ProgressLabel then ProgressLabel.Text = name or "STEALING..." end
    if progressConnection then progressConnection:Disconnect() end
    progressConnection = RunService.Heartbeat:Connect(function()
        if not isStealing then progressConnection:Disconnect() return end
        local prog = math.clamp((tick() - stealStartTime) / Values.STEAL_DURATION, 0, 1)
        if ProgressBarFill      then ProgressBarFill.Size = UDim2.new(prog, 0, 1, 0) end
        if ProgressPercentLabel then ProgressPercentLabel.Text = math.floor(prog * 100) .. "%" end
    end)
    task.spawn(function()
        for _, f in ipairs(data.hold) do task.spawn(f) end
        task.wait(Values.STEAL_DURATION)
        for _, f in ipairs(data.trigger) do task.spawn(f) end
        if progressConnection then progressConnection:Disconnect() end
        ResetProgressBar()
        data.ready = true
        isStealing = false
    end)
end
local function startAutoSteal()
    if Connections.autoSteal then return end
    Connections.autoSteal = RunService.Heartbeat:Connect(function()
        if not Enabled.AutoSteal or isStealing then return end
        local p, _, n = findNearestPrompt()
        if p then executeSteal(p, n) end
    end)
end
local function stopAutoSteal()
    if Connections.autoSteal then Connections.autoSteal:Disconnect() Connections.autoSteal = nil end
    isStealing = false
    ResetProgressBar()
end

-- Steal Paths
local stealPathActive = false
local stealLastFlatVel = Vector3.zero
local STEAL_PATH_VELOCITY_SPEED  = 59.2
local STEAL_PATH_SECOND_SPEED    = 29.6
local STEAL_PATH_BASE_STOP       = 1.35
local STEAL_PATH_MIN_STOP        = 0.65
local STEAL_PATH_NEXT_POINT_BIAS = 0.45
local STEAL_PATH_SMOOTH_FACTOR   = 0.12

local stealPath_Right = {
    {pos = Vector3.new(-470.6, -5.9,  34.4)},
    {pos = Vector3.new(-484.2, -3.9,  21.4)},
    {pos = Vector3.new(-475.6, -5.8,  29.3)},
    {pos = Vector3.new(-473.4, -5.9, 111.0)}
}
local stealPath_Left = {
    {pos = Vector3.new(-474.7, -5.9,  91.0)},
    {pos = Vector3.new(-483.4, -3.9,  97.3)},
    {pos = Vector3.new(-474.7, -5.9,  91.0)},
    {pos = Vector3.new(-476.1, -5.5,  25.4)}
}
local function stealMoveToPoint(hrp, current, nextPoint, speed)
    local conn
    conn = RunService.Heartbeat:Connect(function()
        if not stealPathActive then conn:Disconnect() hrp.AssemblyLinearVelocity = Vector3.zero return end
        local pos = hrp.Position
        local target = Vector3.new(current.X, pos.Y, current.Z)
        local dir = target - pos
        local dist = dir.Magnitude
        local stopDist = math.clamp(STEAL_PATH_BASE_STOP - dist * 0.04, STEAL_PATH_MIN_STOP, STEAL_PATH_BASE_STOP)
        if dist <= stopDist then conn:Disconnect() hrp.AssemblyLinearVelocity = Vector3.zero return end
        local moveDir = dir.Unit
        if nextPoint then
            local nextDir = (Vector3.new(nextPoint.X, pos.Y, nextPoint.Z) - pos).Unit
            moveDir = (moveDir + nextDir * STEAL_PATH_NEXT_POINT_BIAS).Unit
        end
        if stealLastFlatVel.Magnitude > 0.1 then
            moveDir = (moveDir * (1 - STEAL_PATH_SMOOTH_FACTOR) + stealLastFlatVel.Unit * STEAL_PATH_SMOOTH_FACTOR).Unit
        end
        local vel = Vector3.new(moveDir.X * speed, hrp.AssemblyLinearVelocity.Y, moveDir.Z * speed)
        hrp.AssemblyLinearVelocity = vel
        stealLastFlatVel = Vector3.new(vel.X, 0, vel.Z)
    end)
    while stealPathActive and
        (Vector3.new(hrp.Position.X, 0, hrp.Position.Z) - Vector3.new(current.X, 0, current.Z)).Magnitude > STEAL_PATH_BASE_STOP do
        RunService.Heartbeat:Wait()
    end
end
local function stealRunPath(path)
    local hrp = (Player.Character or Player.CharacterAdded:Wait()):WaitForChild("HumanoidRootPart")
    for i, p in ipairs(path) do
        if not stealPathActive then return end
        local speed = i > 2 and STEAL_PATH_SECOND_SPEED or STEAL_PATH_VELOCITY_SPEED
        local nextP = path[i + 1] and path[i + 1].pos
        stealMoveToPoint(hrp, p.pos, nextP, speed)
        if i == 2 then task.wait(0.2) else task.wait(0.01) end
    end
end
local function startStealPath(path)
    stealPathActive = true
    task.spawn(function()
        while stealPathActive do stealRunPath(path) task.wait(0.1) end
    end)
end
local function stopStealPath()
    stealPathActive = false
    local hrp = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.AssemblyLinearVelocity = Vector3.zero end
end

-- ============================================================
-- GUI — Purple Single Column
-- ============================================================
if CoreGui:FindFirstChild("51S_DUELS_UI") then CoreGui["51S_DUELS_UI"]:Destroy() end

local sg = Instance.new("ScreenGui")
sg.Name = "51S_DUELS_UI"
sg.ResetOnSpawn = false
sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
sg.Parent = CoreGui

-- ============================================================
-- PALETTE
-- ============================================================
local PURPLE       = Color3.fromRGB(160, 60, 255)
local PURPLE_LIGHT = Color3.fromRGB(200, 130, 255)
local PURPLE_DARK  = Color3.fromRGB(90, 20, 180)
local BG_DARK      = Color3.fromRGB(10, 4, 20)

-- ============================================================
-- MAIN WINDOW  (single column, 300 wide)
-- ============================================================
local MainContainer = Instance.new("Frame", sg)
MainContainer.Name = "MainContainer"
MainContainer.BackgroundColor3 = BG_DARK
MainContainer.BorderSizePixel  = 0
MainContainer.Size     = UDim2.new(0, 300, 0, 560)
MainContainer.AnchorPoint = Vector2.new(0.5, 0.5)
MainContainer.Position    = UDim2.new(0.5, 0, 0.5, 0)
do
    Instance.new("UICorner", MainContainer).CornerRadius = UDim.new(0, 12)
    local st = Instance.new("UIStroke", MainContainer)
    st.Color = PURPLE  st.Thickness = 2
    st.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local sg2 = Instance.new("UIGradient", st)
    sg2.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,    PURPLE),
        ColorSequenceKeypoint.new(0.25, Color3.fromRGB(0,0,0)),
        ColorSequenceKeypoint.new(0.5,  PURPLE_LIGHT),
        ColorSequenceKeypoint.new(0.75, Color3.fromRGB(0,0,0)),
        ColorSequenceKeypoint.new(1,    PURPLE)
    })
    task.spawn(function()
        local r = 0
        while MainContainer.Parent do
            r = (r + 3) % 360
            sg2.Rotation = r
            task.wait(0.02)
        end
    end)
    -- bg gradient
    local bgFill = Instance.new("Frame", MainContainer)
    bgFill.BackgroundColor3 = BG_DARK bgFill.Size = UDim2.new(1,0,1,0) bgFill.BorderSizePixel = 0
    Instance.new("UICorner", bgFill).CornerRadius = UDim.new(0, 12)
    local grad = Instance.new("UIGradient", bgFill)
    grad.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(18,  6, 36)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(24, 10, 48)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(14,  4, 28))
    }
    grad.Rotation = 90
end

-- Floating particles
for i = 1, 35 do
    local ball = Instance.new("Frame", MainContainer)
    ball.Size = UDim2.new(0, math.random(2,4), 0, math.random(2,4))
    ball.Position = UDim2.new(math.random(2,98)/100, 0, math.random(2,98)/100, 0)
    ball.BackgroundColor3 = math.random(1,2)==1 and PURPLE or PURPLE_LIGHT
    ball.BackgroundTransparency = math.random(40,70)/100
    ball.BorderSizePixel = 0  ball.ZIndex = 1
    Instance.new("UICorner", ball).CornerRadius = UDim.new(1,0)
    task.spawn(function()
        local sx = ball.Position.X.Scale
        local sy = ball.Position.Y.Scale
        local ph = math.random() * math.pi * 2
        local sm = 0.3 + math.random() * 0.4
        while ball.Parent do
            local t = tick() + ph
            ball.Position = UDim2.new(
                math.clamp(sx + math.sin(t * sm) * 0.025, 0.01, 0.99), 0,
                math.clamp(sy + math.cos(t * sm * 0.8) * 0.018, 0.01, 0.99), 0)
            ball.BackgroundTransparency = 0.25 + math.sin(t * 1.5 + ph) * 0.3
            task.wait(0.03)
        end
    end)
end

-- ============================================================
-- LOGO BUTTON (draggable circle, click = pop UI open/close)
-- ============================================================
local LOGO_SIZE = 60
local uiVisible = true

-- Logo sits in sg, completely separate from MainContainer
local Logo = Instance.new("TextButton")
Logo.Name               = "Logo51s"
Logo.Size               = UDim2.new(0, LOGO_SIZE, 0, LOGO_SIZE)
Logo.BackgroundColor3   = Color3.fromRGB(20, 6, 42)
Logo.BorderSizePixel    = 0
Logo.AutoButtonColor    = false
Logo.Text               = ""
Logo.ZIndex             = 50
Logo.ClipsDescendants   = false
Logo.Parent             = sg

-- Position: center of screen
local sv = workspace.CurrentCamera.ViewportSize
Logo.AnchorPoint = Vector2.new(0.5, 0.5)
Logo.Position = UDim2.new(0.5, 0, 0.5, 0)

-- Round
Instance.new("UICorner", Logo).CornerRadius = UDim.new(1, 0)

-- Spinning border
local lStroke = Instance.new("UIStroke", Logo)
lStroke.Thickness = 3
lStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
lStroke.Color = PURPLE
local lGrad = Instance.new("UIGradient", lStroke)
lGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,    PURPLE_LIGHT),
    ColorSequenceKeypoint.new(0.5,  PURPLE_DARK),
    ColorSequenceKeypoint.new(1,    PURPLE_LIGHT),
})
task.spawn(function()
    local r = 0
    while Logo.Parent do
        r = (r + 3) % 360
        lGrad.Rotation = r
        task.wait(0.02)
    end
end)

-- "51s" label
local lText = Instance.new("TextLabel", Logo)
lText.BackgroundTransparency = 1
lText.Size     = UDim2.new(1, 0, 0.55, 0)
lText.Position = UDim2.new(0, 0, 0.06, 0)
lText.Font     = Enum.Font.GothamBold
lText.Text     = "51s"
lText.TextSize = 19
lText.TextColor3 = PURPLE_LIGHT
lText.TextStrokeTransparency = 0.5
lText.ZIndex   = 51

-- "HUB" sub-label
local lSub = Instance.new("TextLabel", Logo)
lSub.BackgroundTransparency = 1
lSub.Size     = UDim2.new(1, 0, 0.3, 0)
lSub.Position = UDim2.new(0, 0, 0.65, 0)
lSub.Font     = Enum.Font.Gotham
lSub.Text     = "HUB"
lSub.TextSize = 9
lSub.TextColor3 = Color3.fromRGB(180, 120, 255)
lSub.ZIndex   = 51

-- Pulse text color
task.spawn(function()
    while lText.Parent do
        local t = tick()
        lText.TextColor3 = Color3.fromRGB(
            math.floor(180 + math.sin(t * 1.1) * 20),
            math.floor(100 + math.sin(t * 0.8) * 30),
            255
        )
        task.wait(0.03)
    end
end)

-- ---- Drag ----
local lDragging  = false
local lMoved     = false
local lDragStart = Vector2.zero
local lPosStart  = Vector2.zero
local THRESHOLD  = 5

Logo.InputBegan:Connect(function(inp)
    if inp.UserInputType == Enum.UserInputType.MouseButton1
    or inp.UserInputType == Enum.UserInputType.Touch then
        lDragging  = true
        lMoved     = false
        lDragStart = Vector2.new(inp.Position.X, inp.Position.Y)
        -- AbsolutePosition is top-left of the button; store center offset
        lPosStart  = Vector2.new(
            Logo.AbsolutePosition.X + LOGO_SIZE / 2,
            Logo.AbsolutePosition.Y + LOGO_SIZE / 2
        )
    end
end)

UserInputService.InputChanged:Connect(function(inp)
    if not lDragging then return end
    if inp.UserInputType ~= Enum.UserInputType.MouseMovement
    and inp.UserInputType ~= Enum.UserInputType.Touch then return end
    local d = Vector2.new(inp.Position.X, inp.Position.Y) - lDragStart
    if d.Magnitude > THRESHOLD then lMoved = true end
    if lMoved then
        local cam = workspace.CurrentCamera.ViewportSize
        local nx = math.clamp(lPosStart.X + d.X, LOGO_SIZE/2, cam.X - LOGO_SIZE/2)
        local ny = math.clamp(lPosStart.Y + d.Y, LOGO_SIZE/2, cam.Y - LOGO_SIZE/2)
        Logo.Position = UDim2.new(0, nx, 0, ny)
    end
end)

-- ---- Click = pop toggle ----
local function setUIVisible(show)
    uiVisible = show
    if show then
        MainContainer.Visible = true
        MainContainer.Size = UDim2.new(0, 300, 0, 0)
        TweenService:Create(MainContainer,
            TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
            { Size = UDim2.new(0, 300, 0, 560) }):Play()
    else
        local tw = TweenService:Create(MainContainer,
            TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.In),
            { Size = UDim2.new(0, 300, 0, 0) })
        tw:Play()
        tw.Completed:Connect(function()
            MainContainer.Visible = false
            MainContainer.Size = UDim2.new(0, 300, 0, 560)
        end)
    end
end

UserInputService.InputEnded:Connect(function(inp)
    if inp.UserInputType ~= Enum.UserInputType.MouseButton1
    and inp.UserInputType ~= Enum.UserInputType.Touch then return end
    if lDragging and not lMoved then
        -- It's a click — toggle
        setUIVisible(not uiVisible)
        -- Logo squish bounce
        TweenService:Create(Logo, TweenInfo.new(0.08), { Size = UDim2.new(0, 46, 0, 46) }):Play()
        task.delay(0.08, function()
            TweenService:Create(Logo,
                TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
                { Size = UDim2.new(0, LOGO_SIZE, 0, LOGO_SIZE) }):Play()
        end)
    end
    lDragging = false
    lMoved    = false
end)

-- ============================================================
-- TITLE HEADER (inside UI, draggable)
-- ============================================================
local TitleHeader = Instance.new("Frame", MainContainer)
TitleHeader.Name = "TitleHeader"
TitleHeader.BackgroundColor3 = Color3.fromRGB(20, 8, 40)
TitleHeader.BorderSizePixel  = 0
TitleHeader.Size     = UDim2.new(1, 0, 0, 44)
TitleHeader.Position = UDim2.new(0, 0, 0, 0)
TitleHeader.ZIndex   = 5
do
    Instance.new("UICorner", TitleHeader).CornerRadius = UDim.new(0, 12)
    -- bottom fill to square off bottom corners visually
    local bottomFill = Instance.new("Frame", TitleHeader)
    bottomFill.BackgroundColor3 = Color3.fromRGB(20, 8, 40)
    bottomFill.BorderSizePixel = 0
    bottomFill.Size = UDim2.new(1, 0, 0, 12)
    bottomFill.Position = UDim2.new(0, 0, 1, -12)
    bottomFill.ZIndex = 4

    -- animated gradient stroke on header
    local hStroke = Instance.new("UIStroke", TitleHeader)
    hStroke.Color = PURPLE  hStroke.Thickness = 1.5
    hStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local hGrad = Instance.new("UIGradient", hStroke)
    hGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   PURPLE),
        ColorSequenceKeypoint.new(0.5, PURPLE_LIGHT),
        ColorSequenceKeypoint.new(1,   PURPLE)
    })
    task.spawn(function()
        local r = 0
        while TitleHeader.Parent do
            r = (r + 2) % 360
            hGrad.Rotation = r
            task.wait(0.02)
        end
    end)

    -- decorative left accent bar
    local accent = Instance.new("Frame", TitleHeader)
    accent.BackgroundColor3 = PURPLE
    accent.BorderSizePixel = 0
    accent.Size = UDim2.new(0, 3, 0, 24)
    accent.Position = UDim2.new(0, 10, 0.5, -12)
    accent.ZIndex = 6
    Instance.new("UICorner", accent).CornerRadius = UDim.new(1, 0)
end

-- Title text label
local titleInner = Instance.new("TextLabel", TitleHeader)
titleInner.BackgroundTransparency = 1
titleInner.Size     = UDim2.new(1, -30, 1, 0)
titleInner.Position = UDim2.new(0, 20, 0, 0)
titleInner.Font     = Enum.Font.GothamBold
titleInner.Text     = "⚡ 51s Duel Hub ⚡"
titleInner.TextColor3 = PURPLE_LIGHT
titleInner.TextSize   = 14
titleInner.TextXAlignment = Enum.TextXAlignment.Left
titleInner.TextStrokeTransparency = 0.5
titleInner.ZIndex   = 7

-- Animate title color pulse
task.spawn(function()
    while titleInner.Parent do
        local t = tick()
        titleInner.TextColor3 = Color3.fromRGB(
            math.floor(180 + math.sin(t * 0.8) * 20),
            math.floor(100 + math.sin(t * 1.1) * 30),
            255
        )
        task.wait(0.03)
    end
end)

-- thin divider line below header
local hDivider = Instance.new("Frame", MainContainer)
hDivider.BackgroundColor3 = PURPLE_DARK
hDivider.BorderSizePixel = 0
hDivider.Size     = UDim2.new(1, -20, 0, 1)
hDivider.Position = UDim2.new(0, 10, 0, 44)
hDivider.ZIndex   = 5

-- Drag (attached to title header)
local dragging, dragInput, dragStart, startPos
TitleHeader.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true  dragStart = input.Position  startPos = MainContainer.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
TitleHeader.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainContainer.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- ============================================================
-- SINGLE SCROLL COLUMN
-- ============================================================
local scroll = Instance.new("ScrollingFrame", MainContainer)
scroll.BackgroundTransparency = 1
scroll.Position = UDim2.new(0, 6, 0, 50)
scroll.Size     = UDim2.new(1, -12, 1, -56)
scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
scroll.ScrollBarThickness = 4
scroll.ScrollBarImageColor3 = PURPLE
scroll.BorderSizePixel = 0  scroll.ZIndex = 2

local layout = Instance.new("UIListLayout", scroll)
layout.Padding = UDim.new(0, 5)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 10)
end)

-- ============================================================
-- SECTION HEADER LABEL
-- ============================================================
local function CreateSectionLabel(text, order)
    local lbl = Instance.new("TextLabel", scroll)
    lbl.BackgroundTransparency = 1
    lbl.Size = UDim2.new(1, -8, 0, 22)
    lbl.Font = Enum.Font.GothamBold
    lbl.Text = text
    lbl.TextColor3 = PURPLE_LIGHT
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.LayoutOrder = order
    lbl.ZIndex = 3
    local ul = Instance.new("Frame", lbl)
    ul.BackgroundColor3 = PURPLE_DARK
    ul.Size = UDim2.new(1, 0, 0, 1)
    ul.Position = UDim2.new(0, 0, 1, -1)
    ul.BorderSizePixel = 0
    return lbl
end

-- ============================================================
-- TOGGLE BUILDER
-- ============================================================
local VisualSetters = {}

local function CreateToggle(labelText, enabledKey, callback, order)
    local row = Instance.new("Frame", scroll)
    row.BackgroundColor3 = Color3.fromRGB(16, 6, 32)
    row.Size = UDim2.new(1, -8, 0, 34)
    row.LayoutOrder = order  row.BorderSizePixel = 0  row.ZIndex = 3
    do
        Instance.new("UICorner", row).CornerRadius = UDim.new(0, 7)
        local st = Instance.new("UIStroke", row) st.Color = PURPLE_DARK st.Thickness = 1.2
    end

    local lbl = Instance.new("TextLabel", row)
    lbl.BackgroundTransparency = 1
    lbl.Size = UDim2.new(0.7, 0, 1, 0)
    lbl.Position = UDim2.new(0, 8, 0, 0)
    lbl.Font = Enum.Font.GothamBold  lbl.Text = labelText
    lbl.TextColor3 = Color3.fromRGB(215, 175, 255)
    lbl.TextSize = 11  lbl.TextXAlignment = Enum.TextXAlignment.Left  lbl.ZIndex = 4

    local isOn = Enabled[enabledKey] or false
    local tBg = Instance.new("Frame", row)
    tBg.Size = UDim2.new(0, 40, 0, 20)
    tBg.Position = UDim2.new(1, -48, 0.5, -10)
    tBg.BackgroundColor3 = isOn and PURPLE or Color3.fromRGB(38, 16, 58)
    tBg.ZIndex = 4
    Instance.new("UICorner", tBg).CornerRadius = UDim.new(1, 0)

    local dot = Instance.new("Frame", tBg)
    dot.Size = UDim2.new(0, 14, 0, 14)
    dot.Position = isOn and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7)
    dot.BackgroundColor3 = Color3.new(1,1,1)  dot.ZIndex = 5
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)

    local btn = Instance.new("TextButton", row)
    btn.Size = UDim2.new(1,0,1,0)  btn.BackgroundTransparency = 1  btn.Text = ""  btn.ZIndex = 6

    local function setVisual(state, skipCb)
        isOn = state
        TweenService:Create(tBg,  TweenInfo.new(0.2), {BackgroundColor3 = isOn and PURPLE or Color3.fromRGB(38,16,58)}):Play()
        TweenService:Create(dot,  TweenInfo.new(0.2, Enum.EasingStyle.Back), {
            Position = isOn and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7)
        }):Play()
        if not skipCb then callback(isOn) end
    end
    VisualSetters[enabledKey] = setVisual
    btn.MouseButton1Click:Connect(function()
        isOn = not isOn  Enabled[enabledKey] = isOn  setVisual(isOn)
    end)
    return row
end

-- ============================================================
-- SLIDER BUILDER
-- ============================================================
local function CreateSlider(labelText, minVal, maxVal, valueKey, callback, order)
    local container = Instance.new("Frame", scroll)
    container.BackgroundColor3 = Color3.fromRGB(16, 6, 32)
    container.Size = UDim2.new(1, -8, 0, 44)
    container.LayoutOrder = order  container.BorderSizePixel = 0  container.ZIndex = 3
    do
        Instance.new("UICorner", container).CornerRadius = UDim.new(0, 7)
        local st = Instance.new("UIStroke", container) st.Color = PURPLE_DARK st.Thickness = 1.2
    end

    local lf = Instance.new("TextLabel", container)
    lf.BackgroundTransparency = 1
    lf.Size = UDim2.new(1, -10, 0, 16)  lf.Position = UDim2.new(0, 8, 0, 4)
    lf.Font = Enum.Font.GothamBold
    lf.Text = labelText .. ": " .. tostring(Values[valueKey])
    lf.TextColor3 = Color3.fromRGB(190, 145, 255)
    lf.TextSize = 10  lf.TextXAlignment = Enum.TextXAlignment.Left  lf.ZIndex = 4

    local sBg = Instance.new("Frame", container)
    sBg.BackgroundColor3 = Color3.fromRGB(12, 4, 22)
    sBg.Position = UDim2.new(0, 8, 0, 26)  sBg.Size = UDim2.new(1, -16, 0, 9)
    sBg.ZIndex = 4
    Instance.new("UICorner", sBg).CornerRadius = UDim.new(1, 0)

    local pct = math.clamp((Values[valueKey] - minVal) / (maxVal - minVal), 0, 1)
    local fill = Instance.new("Frame", sBg)
    fill.BackgroundColor3 = PURPLE  fill.Size = UDim2.new(pct, 0, 1, 0)  fill.ZIndex = 5
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame", sBg)
    knob.BackgroundColor3 = Color3.new(1,1,1)
    knob.Size = UDim2.new(0, 12, 0, 12)  knob.Position = UDim2.new(pct, -6, 0.5, -6)  knob.ZIndex = 6
    do
        Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
        local sk = Instance.new("UIStroke", knob) sk.Color = PURPLE sk.Thickness = 1.5
    end

    local sBtn = Instance.new("TextButton", sBg)
    sBtn.Size = UDim2.new(1,0,3,0)  sBtn.Position = UDim2.new(0,0,-1,0)
    sBtn.BackgroundTransparency = 1  sBtn.Text = ""  sBtn.ZIndex = 7

    local draggingSlider = false
    sBtn.MouseButton1Down:Connect(function() draggingSlider = true end)
    UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
            draggingSlider = false
        end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if draggingSlider and (inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch) then
            local rel = math.clamp((inp.Position.X - sBg.AbsolutePosition.X) / sBg.AbsoluteSize.X, 0, 1)
            fill.Size = UDim2.new(rel, 0, 1, 0)
            knob.Position = UDim2.new(rel, -6, 0.5, -6)
            local val = math.floor(minVal + (maxVal - minVal) * rel)
            Values[valueKey] = val
            lf.Text = labelText .. ": " .. tostring(val)
            callback(val)
        end
    end)
    return container
end

-- ============================================================
-- PROGRESS BAR
-- ============================================================
local pbar = Instance.new("Frame", sg)
pbar.BackgroundColor3 = Color3.fromRGB(12, 5, 24)
pbar.Position = UDim2.new(0.5, -190, 0, 8)
pbar.Size = UDim2.new(0, 380, 0, 42)
pbar.BorderSizePixel = 0  pbar.ZIndex = 10
do
    Instance.new("UICorner", pbar).CornerRadius = UDim.new(0, 8)
    local st = Instance.new("UIStroke", pbar) st.Color = PURPLE st.Thickness = 2
end
ProgressLabel = Instance.new("TextLabel", pbar)
ProgressLabel.BackgroundTransparency = 1
ProgressLabel.Size = UDim2.new(1,0,0,18)  ProgressLabel.Position = UDim2.new(0,10,0,4)
ProgressLabel.Font = Enum.Font.GothamBold  ProgressLabel.Text = "51S DUELS"
ProgressLabel.TextColor3 = Color3.fromRGB(200,150,255)  ProgressLabel.TextSize = 10
ProgressLabel.TextXAlignment = Enum.TextXAlignment.Left  ProgressLabel.ZIndex = 11

local track = Instance.new("Frame", pbar)
track.BackgroundColor3 = Color3.fromRGB(12,4,22)
track.Position = UDim2.new(0,10,0,26)  track.Size = UDim2.new(1,-20,0,11)
track.ZIndex = 11
Instance.new("UICorner", track).CornerRadius = UDim.new(1,0)

ProgressBarFill = Instance.new("Frame", track)
ProgressBarFill.BackgroundColor3 = PURPLE  ProgressBarFill.Size = UDim2.new(0,0,1,0)  ProgressBarFill.ZIndex = 12
Instance.new("UICorner", ProgressBarFill).CornerRadius = UDim.new(1,0)

ProgressPercentLabel = Instance.new("TextLabel", track)
ProgressPercentLabel.BackgroundTransparency = 1  ProgressPercentLabel.Size = UDim2.new(1,0,1,0)
ProgressPercentLabel.Font = Enum.Font.GothamBold  ProgressPercentLabel.Text = ""
ProgressPercentLabel.TextColor3 = Color3.new(1,1,1)  ProgressPercentLabel.TextSize = 9  ProgressPercentLabel.ZIndex = 13

-- ============================================================
-- POPULATE — MOVEMENT SECTION
-- ============================================================
CreateSectionLabel("── MOVEMENT ──", 1)

CreateToggle("Speed Boost [V]", "SpeedBoost", function(s)
    Enabled.SpeedBoost = s
    if s then startSpeedBoost() else stopSpeedBoost() end
end, 2)
CreateSlider("Boost Speed", 1, 70, "BoostSpeed", function(v) Values.BoostSpeed = v end, 3)

CreateToggle("Auto Grab", "AutoSteal", function(s)
    Enabled.AutoSteal = s
    if s then startAutoSteal() else stopAutoSteal() end
end, 4)
CreateSlider("Steal Radius", 5, 100, "STEAL_RADIUS", function(v) Values.STEAL_RADIUS = v end, 5)

CreateToggle("Stealer Speed", "SpeedWhileStealing", function(s)
    Enabled.SpeedWhileStealing = s
    if s then startSpeedWhileStealing() else stopSpeedWhileStealing() end
end, 6)
CreateSlider("Steal Speed", 10, 35, "StealingSpeedValue", function(v) Values.StealingSpeedValue = v end, 7)

CreateToggle("Gravity Mode", "GalaxyMode", function(s)
    Enabled.GalaxyMode = s
    if s then startGalaxyMode() else stopGalaxyMode() end
end, 8)
CreateSlider("Gravity %", 25, 130, "GalaxyGravityPercent", function(v)
    Values.GalaxyGravityPercent = v
    if galaxyModeEnabled then galaxyAdjustJump() end
end, 9)

CreateToggle("Float [F]", "Float", function(s)
    Enabled.Float = s
    if s then startFloat() else stopFloat() end
end, 10)

-- Movement Customizer open button
do
    local mcBtn = Instance.new("TextButton", scroll)
    mcBtn.Name             = "MovementCustomizerBtn"
    mcBtn.Size             = UDim2.new(1, -8, 0, 38)
    mcBtn.LayoutOrder      = 11
    mcBtn.BackgroundColor3 = Color3.fromRGB(28, 8, 55)
    mcBtn.BorderSizePixel  = 0
    mcBtn.AutoButtonColor  = false
    mcBtn.Text             = ""
    mcBtn.ZIndex           = 3
    Instance.new("UICorner", mcBtn).CornerRadius = UDim.new(0, 8)
    local mcStroke = Instance.new("UIStroke", mcBtn)
    mcStroke.Thickness = 1.8
    mcStroke.Color     = PURPLE
    mcStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    -- animated gradient border
    local mcGrad = Instance.new("UIGradient", mcStroke)
    mcGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   PURPLE),
        ColorSequenceKeypoint.new(0.5, PURPLE_LIGHT),
        ColorSequenceKeypoint.new(1,   PURPLE),
    })
    task.spawn(function()
        local r = 0
        while mcBtn.Parent do
            r = (r + 3) % 360
            mcGrad.Rotation = r
            task.wait(0.02)
        end
    end)

    local mcIcon = Instance.new("TextLabel", mcBtn)
    mcIcon.BackgroundTransparency = 1
    mcIcon.Size     = UDim2.new(0, 28, 1, 0)
    mcIcon.Position = UDim2.new(0, 8, 0, 0)
    mcIcon.Font     = Enum.Font.GothamBold
    mcIcon.Text     = "⚙️"
    mcIcon.TextSize = 16
    mcIcon.ZIndex   = 4

    local mcLbl = Instance.new("TextLabel", mcBtn)
    mcLbl.BackgroundTransparency = 1
    mcLbl.Size     = UDim2.new(1, -70, 1, 0)
    mcLbl.Position = UDim2.new(0, 40, 0, 0)
    mcLbl.Font     = Enum.Font.GothamBold
    mcLbl.Text     = "Movement Customizer"
    mcLbl.TextColor3 = PURPLE_LIGHT
    mcLbl.TextSize = 11
    mcLbl.TextXAlignment = Enum.TextXAlignment.Left
    mcLbl.ZIndex   = 4

    local mcArrow = Instance.new("TextLabel", mcBtn)
    mcArrow.BackgroundTransparency = 1
    mcArrow.Size     = UDim2.new(0, 20, 1, 0)
    mcArrow.Position = UDim2.new(1, -26, 0, 0)
    mcArrow.Font     = Enum.Font.GothamBold
    mcArrow.Text     = "▶"
    mcArrow.TextColor3 = PURPLE
    mcArrow.TextSize = 10
    mcArrow.ZIndex   = 4

    -- pulse button glow
    task.spawn(function()
        while mcBtn.Parent do
            local t = tick()
            mcLbl.TextColor3 = Color3.fromRGB(
                math.floor(190 + math.sin(t * 1.0) * 10),
                math.floor(120 + math.sin(t * 0.8) * 20),
                255
            )
            task.wait(0.03)
        end
    end)

    mcBtn.MouseButton1Click:Connect(function()
        -- toggle panel (defined below, referenced via upvalue)
        if _G.SpeedPanelRef then
            local p = _G.SpeedPanelRef
            if p.Visible then
                TweenService:Create(p, TweenInfo.new(0.2, Enum.EasingStyle.Back, Enum.EasingDirection.In),
                    {Size = UDim2.new(0, p.Size.X.Offset, 0, 0)}):Play()
                task.delay(0.2, function() p.Visible = false end)
                mcArrow.Text = "▶"
            else
                p.Visible = true
                p.Size = UDim2.new(0, p.Size.X.Offset, 0, 0)
                TweenService:Create(p, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
                    {Size = UDim2.new(0, p.Size.X.Offset, 0, _G.SpeedPanelFullH)}):Play()
                mcArrow.Text = "▼"
            end
        end
        -- bounce
        TweenService:Create(mcBtn, TweenInfo.new(0.07), {Size = UDim2.new(1, -12, 0, 33)}):Play()
        task.delay(0.07, function()
            TweenService:Create(mcBtn, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
                {Size = UDim2.new(1, -8, 0, 38)}):Play()
        end)
    end)
end

-- ============================================================
-- POPULATE — COMBAT SECTION
-- ============================================================
CreateSectionLabel("── COMBAT ──", 11)

CreateToggle("Hit Circle", "HitCircle", function(s)
    Enabled.HitCircle = s
    if s then startHitCircle() else stopHitCircle() end
end, 12)

CreateToggle("Anti Ragdoll", "AntiRagdoll", function(s)
    Enabled.AntiRagdoll = s
    if s then startAntiRagdoll() else stopAntiRagdoll() end
end, 13)

CreateToggle("Spam Bat", "SpamBat", function(s)
    Enabled.SpamBat = s
    if s then startSpamBat() else stopSpamBat() end
end, 14)

CreateToggle("Spin Bot", "Helicopter", function(s)
    Enabled.Helicopter = s
    if s then startHelicopter() else stopHelicopter() end
end, 15)
CreateSlider("Spin Speed", 5, 80, "SpinSpeed", function(v)
    Values.SpinSpeed = v
    if helicopterBAV then helicopterBAV.AngularVelocity = Vector3.new(0, v, 0) end
end, 16)

CreateToggle("Unwalk", "Unwalk", function(s)
    Enabled.Unwalk = s
    if s then startUnwalk() else stopUnwalk() end
end, 17)

CreateToggle("Optimizer + XRay", "OptimizerXRay", function(s)
    Enabled.OptimizerXRay = s
    if s then enableOptimizer() enableXRay() else disableOptimizer() disableXRay() end
end, 18)

CreateToggle("💜 Purple Vibe", "Vibrance", function(s)
    Enabled.Vibrance = s
    if s then enablePurpleVibe() else disablePurpleVibe() end
end, 19)

CreateToggle("Auto Right Steal [E]", "AutoRight", function(s)
    Enabled.AutoRight = s
    if s then
        Enabled.AutoLeft = false
        if VisualSetters.AutoLeft then VisualSetters.AutoLeft(false, true) end
        stopStealPath() startStealPath(stealPath_Right)
    else stopStealPath() end
end, 20)

CreateToggle("Auto Left Steal [Q]", "AutoLeft", function(s)
    Enabled.AutoLeft = s
    if s then
        Enabled.AutoRight = false
        if VisualSetters.AutoRight then VisualSetters.AutoRight(false, true) end
        stopStealPath() startStealPath(stealPath_Left)
    else stopStealPath() end
end, 21)

-- ============================================================
-- FLOATING OUTSIDE BUTTONS (Auto Steal, Auto Right, Auto Left)
-- ============================================================
local function makeFloatingBtn(label, sublabel, posX, posY, color, onToggle)
    local btn = Instance.new("TextButton", sg)
    btn.Size = UDim2.new(0, 90, 0, 50)
    btn.Position = UDim2.new(0, posX, 0, posY)
    btn.AnchorPoint = Vector2.new(0.5, 0.5)
    btn.BackgroundColor3 = Color3.fromRGB(14, 5, 30)
    btn.BorderSizePixel = 0
    btn.AutoButtonColor = false
    btn.Text = ""
    btn.ZIndex = 30
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    local stroke = Instance.new("UIStroke", btn)
    stroke.Thickness = 2
    stroke.Color = color
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    local topLbl = Instance.new("TextLabel", btn)
    topLbl.BackgroundTransparency = 1
    topLbl.Size = UDim2.new(1, 0, 0.55, 0)
    topLbl.Position = UDim2.new(0, 0, 0.04, 0)
    topLbl.Font = Enum.Font.GothamBold
    topLbl.Text = label
    topLbl.TextColor3 = Color3.fromRGB(215, 175, 255)
    topLbl.TextSize = 12
    topLbl.ZIndex = 31

    local subLbl = Instance.new("TextLabel", btn)
    subLbl.BackgroundTransparency = 1
    subLbl.Size = UDim2.new(1, 0, 0.36, 0)
    subLbl.Position = UDim2.new(0, 0, 0.6, 0)
    subLbl.Font = Enum.Font.Gotham
    subLbl.Text = sublabel
    subLbl.TextColor3 = Color3.fromRGB(130, 80, 200)
    subLbl.TextSize = 9
    subLbl.ZIndex = 31

    -- status dot
    local dot = Instance.new("Frame", btn)
    dot.Size = UDim2.new(0, 7, 0, 7)
    dot.Position = UDim2.new(1, -10, 0, 5)
    dot.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
    dot.BorderSizePixel = 0
    dot.ZIndex = 32
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)

    local isOn = false
    local function setOn(on, skipCb)
        isOn = on
        TweenService:Create(btn, TweenInfo.new(0.15), {
            BackgroundColor3 = on and Color3.fromRGB(50, 15, 100) or Color3.fromRGB(14, 5, 30)
        }):Play()
        TweenService:Create(stroke, TweenInfo.new(0.15), {
            Color = on and PURPLE_LIGHT or color
        }):Play()
        TweenService:Create(dot, TweenInfo.new(0.15), {
            BackgroundColor3 = on and Color3.fromRGB(80, 255, 120) or Color3.fromRGB(80, 80, 80)
        }):Play()
        topLbl.TextColor3 = on and Color3.new(1,1,1) or Color3.fromRGB(215, 175, 255)
        if not skipCb then onToggle(on) end
    end

    local isDragging = false
    local hasMoved   = false
    local dStart     = Vector2.zero
    local pStart     = Vector2.zero
    local DRAG_T     = 5

    btn.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1
        or inp.UserInputType == Enum.UserInputType.Touch then
            isDragging = true
            hasMoved   = false
            dStart     = Vector2.new(inp.Position.X, inp.Position.Y)
            pStart     = Vector2.new(
                btn.AbsolutePosition.X + btn.AbsoluteSize.X / 2,
                btn.AbsolutePosition.Y + btn.AbsoluteSize.Y / 2
            )
        end
    end)

    UserInputService.InputChanged:Connect(function(inp)
        if not isDragging then return end
        if inp.UserInputType ~= Enum.UserInputType.MouseMovement
        and inp.UserInputType ~= Enum.UserInputType.Touch then return end
        local d = Vector2.new(inp.Position.X, inp.Position.Y) - dStart
        if d.Magnitude > DRAG_T then hasMoved = true end
        if hasMoved then
            local cam = workspace.CurrentCamera.ViewportSize
            local nx = math.clamp(pStart.X + d.X, btn.AbsoluteSize.X/2, cam.X - btn.AbsoluteSize.X/2)
            local ny = math.clamp(pStart.Y + d.Y, btn.AbsoluteSize.Y/2, cam.Y - btn.AbsoluteSize.Y/2)
            btn.Position = UDim2.new(0, nx, 0, ny)
        end
    end)

    UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType ~= Enum.UserInputType.MouseButton1
        and inp.UserInputType ~= Enum.UserInputType.Touch then return end
        if isDragging and not hasMoved then
            -- click
            setOn(not isOn)
            TweenService:Create(btn, TweenInfo.new(0.07), {Size = UDim2.new(0, 80, 0, 44)}):Play()
            task.delay(0.07, function()
                TweenService:Create(btn, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
                    {Size = UDim2.new(0, 90, 0, 50)}):Play()
            end)
        end
        isDragging = false
        hasMoved   = false
    end)

    return btn, setOn
end

-- Place them stacked on the right side of screen
local camV = workspace.CurrentCamera.ViewportSize
local rightX = camV.X - 58
local baseY  = camV.Y - 190

-- Pre-declare so callbacks can reference each other
local setAutoRightOn, setAutoLeftOn

local autoRightBtn
autoRightBtn, setAutoRightOn = makeFloatingBtn(
    "▶ RIGHT", "[E] Path", rightX, baseY,
    Color3.fromRGB(100, 200, 255), function(on)
        Enabled.AutoRight = on
        if on then
            Enabled.AutoLeft = false
            if setAutoLeftOn then setAutoLeftOn(false, true) end
            stopStealPath()
            startStealPath(stealPath_Right)
        else
            stopStealPath()
        end
    end
)
VisualSetters.AutoRight = function(on, skip) setAutoRightOn(on, skip) end

local autoLeftBtn
autoLeftBtn, setAutoLeftOn = makeFloatingBtn(
    "◀ LEFT", "[Q] Path", rightX, baseY + 62,
    Color3.fromRGB(255, 150, 100), function(on)
        Enabled.AutoLeft = on
        if on then
            Enabled.AutoRight = false
            if setAutoRightOn then setAutoRightOn(false, true) end
            stopStealPath()
            startStealPath(stealPath_Left)
        else
            stopStealPath()
        end
    end
)
VisualSetters.AutoLeft = function(on, skip) setAutoLeftOn(on, skip) end

local floatBtn, setFloatOn = makeFloatingBtn(
    "🪂 FLOAT", "[F] Key", rightX, baseY + 124,
    Color3.fromRGB(120, 255, 200), function(on)
        Enabled.Float = on
        if on then startFloat() else stopFloat() end
    end
)
VisualSetters.Float = function(on, skip) setFloatOn(on, skip) end

-- ============================================================
-- SPEED / GRAVITY FLOATING PANEL (draggable)
-- ============================================================
do
    local PANEL_W = 260
    local PANEL_H = 220
    local camV2   = workspace.CurrentCamera.ViewportSize

    local panel = Instance.new("Frame", sg)
    panel.Name             = "SpeedPanel"
    panel.Size             = UDim2.new(0, PANEL_W, 0, PANEL_H)
    panel.AnchorPoint      = Vector2.new(0.5, 0.5)
    panel.Position         = UDim2.new(0.5, 0, 0.5, 0)
    panel.BackgroundColor3 = Color3.fromRGB(12, 4, 24)
    panel.BorderSizePixel  = 0
    panel.ZIndex           = 30
    panel.Visible          = false
    _G.SpeedPanelRef       = panel
    _G.SpeedPanelFullH     = PANEL_H
    Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 12)
    local pStroke = Instance.new("UIStroke", panel)
    pStroke.Thickness = 2
    pStroke.Color = PURPLE
    pStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    local pGrad = Instance.new("UIGradient", pStroke)
    pGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, PURPLE),
        ColorSequenceKeypoint.new(0.5, PURPLE_LIGHT),
        ColorSequenceKeypoint.new(1, PURPLE),
    })
    task.spawn(function()
        local r = 0
        while panel.Parent do
            r = (r + 2) % 360
            pGrad.Rotation = r
            task.wait(0.03)
        end
    end)

    -- Panel title / drag handle
    local titleBar = Instance.new("TextButton", panel)
    titleBar.Size             = UDim2.new(1, 0, 0, 28)
    titleBar.Position         = UDim2.new(0, 0, 0, 0)
    titleBar.BackgroundColor3 = Color3.fromRGB(22, 8, 44)
    titleBar.BorderSizePixel  = 0
    titleBar.AutoButtonColor  = false
    titleBar.Text             = ""
    titleBar.ZIndex           = 31
    Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 10)
    -- square off bottom corners
    local tbFill = Instance.new("Frame", titleBar)
    tbFill.BackgroundColor3 = Color3.fromRGB(22, 8, 44)
    tbFill.BorderSizePixel  = 0
    tbFill.Size             = UDim2.new(1, 0, 0, 10)
    tbFill.Position         = UDim2.new(0, 0, 1, -10)
    tbFill.ZIndex           = 31

    local titleLblP = Instance.new("TextLabel", titleBar)
    titleLblP.BackgroundTransparency = 1
    titleLblP.Size     = UDim2.new(1, -80, 1, 0)
    titleLblP.Position = UDim2.new(0, 10, 0, 0)
    titleLblP.Font     = Enum.Font.GothamBold
    titleLblP.Text     = "⚙️ Movement Customizer"
    titleLblP.TextColor3 = PURPLE_LIGHT
    titleLblP.TextSize = 11
    titleLblP.TextXAlignment = Enum.TextXAlignment.Left
    titleLblP.ZIndex   = 32

    -- ON indicator dot (always visible even when minimized)
    local onDot = Instance.new("Frame", titleBar)
    onDot.Size             = UDim2.new(0, 8, 0, 8)
    onDot.Position         = UDim2.new(1, -52, 0.5, -4)
    onDot.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    onDot.BorderSizePixel  = 0
    onDot.ZIndex           = 33
    Instance.new("UICorner", onDot).CornerRadius = UDim.new(1, 0)

    local onLbl = Instance.new("TextLabel", titleBar)
    onLbl.BackgroundTransparency = 1
    onLbl.Size     = UDim2.new(0, 24, 1, 0)
    onLbl.Position = UDim2.new(1, -62, 0, 0)
    onLbl.Font     = Enum.Font.GothamBold
    onLbl.Text     = "ON"
    onLbl.TextColor3 = Color3.fromRGB(60, 60, 60)
    onLbl.TextSize = 9
    onLbl.ZIndex   = 33

    -- update onDot whenever a feature in this panel is toggled
    local function refreshOnDot()
        local anyOn = Enabled.SpeedBoost or Enabled.SpeedWhileStealing or Enabled.GalaxyMode
        TweenService:Create(onDot, TweenInfo.new(0.2), {
            BackgroundColor3 = anyOn and Color3.fromRGB(80, 255, 120) or Color3.fromRGB(60, 60, 60)
        }):Play()
        onLbl.TextColor3 = anyOn and Color3.fromRGB(80, 255, 120) or Color3.fromRGB(60, 60, 60)
    end
    _G.RefreshSpeedPanelDot = refreshOnDot

    -- Minimize button
    local minBtn = Instance.new("TextButton", titleBar)
    minBtn.Size             = UDim2.new(0, 26, 0, 20)
    minBtn.Position         = UDim2.new(1, -30, 0.5, -10)
    minBtn.BackgroundColor3 = Color3.fromRGB(40, 12, 70)
    minBtn.BorderSizePixel  = 0
    minBtn.AutoButtonColor  = false
    minBtn.Font             = Enum.Font.GothamBold
    minBtn.Text             = "—"
    minBtn.TextColor3       = PURPLE_LIGHT
    minBtn.TextSize         = 12
    minBtn.ZIndex           = 34
    Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 6)

    local minimized = false
    local contentFrames = {} -- populated after sliders/toggles are created

    local function setMinimized(min)
        minimized = min
        minBtn.Text = min and "□" or "—"
        if min then
            TweenService:Create(panel, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
                { Size = UDim2.new(0, PANEL_W, 0, 28) }):Play()
        else
            TweenService:Create(panel, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
                { Size = UDim2.new(0, PANEL_W, 0, PANEL_H) }):Play()
        end
        -- hide/show content (everything except titleBar)
        for _, child in ipairs(panel:GetChildren()) do
            if child ~= titleBar and child:IsA("GuiObject") then
                child.Visible = not min
            end
        end
    end

    minBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end)

    -- Drag on title bar
    local pDragging = false
    local pMoved    = false
    local pDStart   = Vector2.zero
    local pPStart   = Vector2.zero

    titleBar.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1
        or inp.UserInputType == Enum.UserInputType.Touch then
            pDragging = true
            pMoved    = false
            pDStart   = Vector2.new(inp.Position.X, inp.Position.Y)
            pPStart   = Vector2.new(panel.AbsolutePosition.X + PANEL_W/2, panel.AbsolutePosition.Y + PANEL_H/2)
        end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if not pDragging then return end
        if inp.UserInputType ~= Enum.UserInputType.MouseMovement
        and inp.UserInputType ~= Enum.UserInputType.Touch then return end
        local d = Vector2.new(inp.Position.X, inp.Position.Y) - pDStart
        if d.Magnitude > 4 then pMoved = true end
        if pMoved then
            local cv = workspace.CurrentCamera.ViewportSize
            local nx = math.clamp(pPStart.X + d.X, PANEL_W/2, cv.X - PANEL_W/2)
            local ny = math.clamp(pPStart.Y + d.Y, PANEL_H/2, cv.Y - PANEL_H/2)
            panel.Position = UDim2.new(0, nx, 0, ny)
        end
    end)
    UserInputService.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1
        or inp.UserInputType == Enum.UserInputType.Touch then
            pDragging = false
            pMoved    = false
        end
    end)

    -- Helper: create a slider row inside the panel
    local function makePanelSlider(labelText, yOff, minV, maxV, valueKey, color, onChange)
        local rowLbl = Instance.new("TextLabel", panel)
        rowLbl.BackgroundTransparency = 1
        rowLbl.Size     = UDim2.new(1, -16, 0, 14)
        rowLbl.Position = UDim2.new(0, 8, 0, yOff)
        rowLbl.Font     = Enum.Font.GothamBold
        rowLbl.Text     = labelText .. ": " .. tostring(Values[valueKey])
        rowLbl.TextColor3 = color
        rowLbl.TextSize = 10
        rowLbl.TextXAlignment = Enum.TextXAlignment.Left
        rowLbl.ZIndex   = 32

        local track = Instance.new("Frame", panel)
        track.BackgroundColor3 = Color3.fromRGB(20, 8, 38)
        track.BorderSizePixel  = 0
        track.Size     = UDim2.new(1, -16, 0, 8)
        track.Position = UDim2.new(0, 8, 0, yOff + 16)
        track.ZIndex   = 32
        Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

        local pct = math.clamp((Values[valueKey] - minV) / (maxV - minV), 0, 1)
        local fill = Instance.new("Frame", track)
        fill.BackgroundColor3 = color
        fill.BorderSizePixel  = 0
        fill.Size     = UDim2.new(pct, 0, 1, 0)
        fill.ZIndex   = 33
        Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)

        local knob = Instance.new("Frame", track)
        knob.BackgroundColor3 = Color3.new(1, 1, 1)
        knob.BorderSizePixel  = 0
        knob.Size     = UDim2.new(0, 12, 0, 12)
        knob.Position = UDim2.new(pct, -6, 0.5, -6)
        knob.ZIndex   = 34
        Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
        local ks = Instance.new("UIStroke", knob) ks.Color = color ks.Thickness = 1.5

        local hitbox = Instance.new("TextButton", track)
        hitbox.Size = UDim2.new(1, 0, 3, 0)
        hitbox.Position = UDim2.new(0, 0, -1, 0)
        hitbox.BackgroundTransparency = 1
        hitbox.Text = ""
        hitbox.ZIndex = 35

        local sliding = false
        hitbox.InputBegan:Connect(function(inp)
            if inp.UserInputType == Enum.UserInputType.MouseButton1
            or inp.UserInputType == Enum.UserInputType.Touch then sliding = true end
        end)
        UserInputService.InputEnded:Connect(function(inp)
            if inp.UserInputType == Enum.UserInputType.MouseButton1
            or inp.UserInputType == Enum.UserInputType.Touch then sliding = false end
        end)
        UserInputService.InputChanged:Connect(function(inp)
            if not sliding then return end
            if inp.UserInputType ~= Enum.UserInputType.MouseMovement
            and inp.UserInputType ~= Enum.UserInputType.Touch then return end
            local rel = math.clamp((inp.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
            fill.Size     = UDim2.new(rel, 0, 1, 0)
            knob.Position = UDim2.new(rel, -6, 0.5, -6)
            local val = math.floor(minV + (maxV - minV) * rel)
            Values[valueKey] = val
            rowLbl.Text = labelText .. ": " .. tostring(val)
            onChange(val)
        end)
    end

    -- Toggle row helper
    local function makePanelToggle(labelText, yOff, enabledKey, color, onCb)
        local tLbl = Instance.new("TextLabel", panel)
        tLbl.BackgroundTransparency = 1
        tLbl.Size     = UDim2.new(0.65, 0, 0, 18)
        tLbl.Position = UDim2.new(0, 8, 0, yOff)
        tLbl.Font     = Enum.Font.GothamBold
        tLbl.Text     = labelText
        tLbl.TextColor3 = color
        tLbl.TextSize = 10
        tLbl.TextXAlignment = Enum.TextXAlignment.Left
        tLbl.ZIndex   = 32

        local tBg = Instance.new("Frame", panel)
        tBg.Size     = UDim2.new(0, 34, 0, 17)
        tBg.Position = UDim2.new(1, -42, 0, yOff + 1)
        tBg.BackgroundColor3 = Color3.fromRGB(38, 16, 58)
        tBg.BorderSizePixel  = 0
        tBg.ZIndex   = 32
        Instance.new("UICorner", tBg).CornerRadius = UDim.new(1, 0)

        local dot = Instance.new("Frame", tBg)
        dot.Size     = UDim2.new(0, 11, 0, 11)
        dot.Position = UDim2.new(0, 3, 0.5, -5.5)
        dot.BackgroundColor3 = Color3.new(1, 1, 1)
        dot.BorderSizePixel  = 0
        dot.ZIndex   = 33
        Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)

        local isOn = Enabled[enabledKey] or false
        local function setIt(on, skipCb)
            isOn = on
            TweenService:Create(tBg, TweenInfo.new(0.18), {BackgroundColor3 = on and color or Color3.fromRGB(38,16,58)}):Play()
            TweenService:Create(dot, TweenInfo.new(0.18, Enum.EasingStyle.Back), {
                Position = on and UDim2.new(1,-14,0.5,-5.5) or UDim2.new(0,3,0.5,-5.5)
            }):Play()
            if not skipCb then Enabled[enabledKey] = on onCb(on) end
        end
        VisualSetters[enabledKey] = function(on, skip) setIt(on, skip) end

        local tBtn = Instance.new("TextButton", panel)
        tBtn.Size     = UDim2.new(1, 0, 0, 20)
        tBtn.Position = UDim2.new(0, 0, 0, yOff)
        tBtn.BackgroundTransparency = 1
        tBtn.Text     = ""
        tBtn.ZIndex   = 35
        tBtn.MouseButton1Click:Connect(function()
            isOn = not isOn
            Enabled[enabledKey] = isOn
            setIt(isOn)
        end)
    end

    -- Build the rows (y positions inside panel, starting after titleBar)
    makePanelToggle("⚡ Speed Boost [V]", 36, "SpeedBoost", PURPLE, function(on)
        if on then startSpeedBoost() else stopSpeedBoost() end
        refreshOnDot()
    end)
    makePanelSlider("Boost Speed", 58, 1, 70, "BoostSpeed", PURPLE, function(v)
        Values.BoostSpeed = v
    end)

    makePanelToggle("💨 Stealer Speed", 96, "SpeedWhileStealing", Color3.fromRGB(100, 200, 255), function(on)
        if on then startSpeedWhileStealing() else stopSpeedWhileStealing() end
        refreshOnDot()
    end)
    makePanelSlider("Steal Speed", 118, 10, 35, "StealingSpeedValue", Color3.fromRGB(100, 200, 255), function(v)
        Values.StealingSpeedValue = v
    end)

    makePanelToggle("🌌 Gravity Mode", 156, "GalaxyMode", Color3.fromRGB(255, 180, 80), function(on)
        if on then startGalaxyMode() else stopGalaxyMode() end
        refreshOnDot()
    end)
    makePanelSlider("Gravity %", 178, 25, 130, "GalaxyGravityPercent", Color3.fromRGB(255, 180, 80), function(v)
        Values.GalaxyGravityPercent = v
        if galaxyModeEnabled then galaxyAdjustJump() end
    end)
end

-- ============================================================
-- KEYBINDS & RESPAWN
-- ============================================================
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.Delete then
        sg:Destroy()
    elseif input.KeyCode == Enum.KeyCode.U then
        setUIVisible(not uiVisible)
    elseif input.KeyCode == KEYBINDS.SPEED then
        Enabled.SpeedBoost = not Enabled.SpeedBoost
        if VisualSetters.SpeedBoost then VisualSetters.SpeedBoost(Enabled.SpeedBoost) end
        if Enabled.SpeedBoost then startSpeedBoost() else stopSpeedBoost() end
    elseif input.KeyCode == KEYBINDS.FLOAT then
        Enabled.Float = not Enabled.Float
        if VisualSetters.Float then VisualSetters.Float(Enabled.Float) end
        if Enabled.Float then startFloat() else stopFloat() end
    elseif input.KeyCode == KEYBINDS.AUTORIGHT then
        Enabled.AutoRight = not Enabled.AutoRight
        if Enabled.AutoRight then
            Enabled.AutoLeft = false
            if VisualSetters.AutoLeft  then VisualSetters.AutoLeft(false, true) end
            if VisualSetters.AutoRight then VisualSetters.AutoRight(true, true) end
            stopStealPath()  startStealPath(stealPath_Right)
        else
            if VisualSetters.AutoRight then VisualSetters.AutoRight(false, true) end
            stopStealPath()
        end
    elseif input.KeyCode == KEYBINDS.AUTOLEFT then
        Enabled.AutoLeft = not Enabled.AutoLeft
        if Enabled.AutoLeft then
            Enabled.AutoRight = false
            if VisualSetters.AutoRight then VisualSetters.AutoRight(false, true) end
            if VisualSetters.AutoLeft  then VisualSetters.AutoLeft(true, true) end
            stopStealPath()  startStealPath(stealPath_Left)
        else
            if VisualSetters.AutoLeft then VisualSetters.AutoLeft(false, true) end
            stopStealPath()
        end
    end
end)

Player.CharacterAdded:Connect(function()
    task.wait(1)
    if Enabled.SpeedBoost         then startSpeedBoost() end
    if Enabled.AutoSteal          then startAutoSteal() end
    if Enabled.SpeedWhileStealing then startSpeedWhileStealing() end
    if Enabled.AntiRagdoll        then startAntiRagdoll() end
    if Enabled.HitCircle          then stopHitCircle() task.wait(0.1) startHitCircle() end
    if Enabled.SpamBat            then startSpamBat() end
    if Enabled.Helicopter         then startHelicopter() end
end)